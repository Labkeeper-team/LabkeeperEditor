import { Typography } from '../../components/typography';
import { colors } from '../../styles/colors';

export const TokensPage = () => {
    return (
        <div style={{ padding: 24 }}>
            <Typography type="body-large" color={colors.gray10} text="Tokens" />
        </div>
    );
};
